
struct SizedBox {
    var width = 100
    var height = 100
    var child

    func render() {
        // return "<div style='width: 100px; height: 100px;'>" + child.render() + "</div>"
        return "test"
    }
}

func App() {
    var box = SizedBox()
    box.width = 200
    box.height = 200

    return box
}

func Render() {
    return App().render()
}