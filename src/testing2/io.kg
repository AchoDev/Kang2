

func print(value) {
    log value
}

func input(value) {
    var result
    prompt value -> result
    return result
}

struct House {
    var name = "building"
    var color = "red"

    func get_infos() {
        print("The " +  self.name +  "house is " + self.color)
    }
}


