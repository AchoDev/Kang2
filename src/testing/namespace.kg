
struct io {
    static func print(value) {
        log value
    }

    static func input(question) {
        var result = ""
        prompt question -> result
        return result
    }
}

io.print("test")

var res = io.input("whats your name? ")

io.print("hello, " + res + ":)")

io.input("press enter to exit")